Count how many times a specified character appears in a given string.
(e.g.: I/P: "apple",Char: 'p' ,O/P: "2")

Coding //
#include <stdio.h>
#include<string.h>
int main()
{
    char str[100],ch;
    int count=0;
    printf("Enter a String: ");
    fgets(str,sizeof(str),stdin);
    printf("Enter a Character :");
    scanf("%c",&ch);
    int len=strlen(str);
    for(int i=0;i<len;i++)
    {
        if(str[i]==ch)
    {
        count++;
    }
}
printf("\"%d\"",count);
return 0;
}

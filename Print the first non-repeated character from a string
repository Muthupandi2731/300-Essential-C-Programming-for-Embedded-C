Examine the string and pinpoint the very first character that doesn't repeat elsewhere
in the string.
(e.g.: I/P: "swiss" ,O/P: "w")

coding//
#include<stdio.h>
#include<string.h>
int main()
{
    char str[100];
    int count=0,num;
    scanf("%s",str);
    int len=strlen(str);
    char ch;
    for(int i=0;i<len;i++)
    {
        count=0;
        for(int j=1;j<len;j++)
        {
            if(str[i]==str[j])
            {
                count++;
            }
        }
        if(count==0)
        {
            ch=str[i];
            num=1;
            break;
        }
    }
    if(num==1)
    {
    printf("The first Non repeating Character is %c",ch);
    }else
    {
        printf("All are repeated");
    }
}
